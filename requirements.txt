fastapi
uvicorn[standard]
insightface
opencv-python-headless
numpy==1.26.4
psycopg2-binary
python-multipart
Pillow
onnxruntime==1.19.0
python-dotenv
SQLAlchemy
alembic
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
email-validator==2.2.0
pydantic-settings
dotenv
# Use CPU-only PyTorch wheels on Railway Linux to prevent CUDA package downloads.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu; platform_system == "Linux" and platform_machine == "x86_64"
torchvision==0.20.1+cpu; platform_system == "Linux" and platform_machine == "x86_64"
ultralytics==8.4.20
easyocr

# Optional: for SMS_PROVIDER=twilio
twilio

# For tests (tests/admin/*.py hit the API with requests)
requests
pytest
httpx
